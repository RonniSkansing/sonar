-  MONITOR_NAME:
    host: example.com // * required
    type: http        // default to https
    interval: 5s      // default to 5s
    report:           // default to file flat ./MONITOR_NAME
      type: file
      format: flat
      path: ./ 

    auth:             // [OPTIONAL]
      type: basicauth
      username: foo
      password: bar